/* Enable the Icinga DB feature: icinga2 publishes its state to redis,
 * from where the icingadb daemon drains it into MariaDB. */
library "icingadb"

object IcingaDB "icingadb" {
  host = "icingadb-redis"
  port = 6379
}
