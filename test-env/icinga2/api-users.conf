/* Known API credentials so the test scripts and icingadb-web command
 * transport can talk to the Icinga 2 API (root / icinga). */
object ApiUser "root" {
  password = "icinga"
  permissions = [ "*" ]
}
